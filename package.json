{
  "name": "backend-players",
  "version": "1.0.0",
  "description": "this is for backend players",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "preeti",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "expres": "^0.0.5",
    "express": "^4.21.2"
  }
}
